pipeline {
        agent any
        stages {
          
          stage("build & SonarQube Scan") {
            agent any
            steps {
              withSonarQubeEnv('SonarQube_Server') {
                sh 'mvn clean package sonar:sonar'
              }
            }
          }
        }
      }
